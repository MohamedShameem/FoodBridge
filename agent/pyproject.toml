[project]
name = "foodbridge-agent"
version = "0.1.0"
description = "Strands agent for autonomous surplus-food rescue coordination"
requires-python = ">=3.13"
dependencies = [
  "bedrock-agentcore>=1.0.0",
  "strands-agents>=1.0.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
